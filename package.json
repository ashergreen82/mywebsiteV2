{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Django personal website deployed on Netlify",
  "main": "index.js",
  "scripts": {
    "build": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "serverless-http": "^3.2.0"
  }
}
